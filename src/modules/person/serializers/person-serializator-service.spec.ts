import { PersonSerializatorService } from './person-serializator-service';

describe('PersonSerializatorService', () => {
  it('should be defined', () => {
    expect(new PersonSerializatorService()).toBeDefined();
  });
});
